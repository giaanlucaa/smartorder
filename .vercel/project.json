{
  "orgId": "vercel",
  "projectId": "smartorder",
  "settings": {
    "buildCommand": "pnpm --filter web build",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "outputDirectory": "apps/web/.next",
    "framework": "nextjs"
  }
}
